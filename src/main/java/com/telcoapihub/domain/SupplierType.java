package com.telcoapihub.domain;

/**
 * Southbound supply is hybrid: aggregators now, direct operator
 * integrations added incrementally.
 */
public enum SupplierType {
    OPERATOR,
    AGGREGATOR
}
